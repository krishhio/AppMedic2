import React, { ReactElement } from 'react';

type Props = {
  msg: string;
  title: string;
  subTitle: string;
  img: ReactElement;
  bg: ReactElement;
};

const BaseErrorPage = () => <>
  
</>;

export default BaseErrorPage;
